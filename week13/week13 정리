TinyOS 기반 NesC 프로그래밍 (온습도센서)

직비무선으로 데이터 받는 프로그램 
TestAppC.nc <- 와이어링용

무선데이터 전송을 위한 기본 프로토콜
ActiveMessageC, AMC

#ifndef, #endif
- 중복을 방지하기 우해서 사용

enum, const는 비슷한 역할을 함

const 같은 코드상에서 사용
enum은 해더파일에서 사용

TestC.nc는 시간단위로 실행되기때문에 오류가 발생하든 않하든 계속 진행됨

test_data_msg_t는 test.h에서 가져온 구조체임

함수는 동기식 비동기식 함수가 있음

비동기식 함수는 호출하는 동안 다른 실행문이 수행됨
evnet는 비동기식 함수

:(숫자)치면 해당 줄번호로 이동됨

무선으로 전송되는 데이터는 절대 100%로는 전송안됨
